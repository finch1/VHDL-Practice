
case "${N2GNU_BUILD_NEWLIB}" in
  normalc) . ${newlib_basedir}/configure.host.normalc ;; 
  smallc)  . ${newlib_basedir}/configure.host.smallc ;; 
  *)       { # configure.host.warning
             echo '***' "configure.host is all hacked up,"  1>&2
             echo '***' "you need to choose one of configure.host.smallc" 1>&2
             echo '***' "or configure.host.normalc and replace" 1>&2
             echo '***' "configure.host" 1>&2
             exit 1; } ;;
esac
